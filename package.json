{
	"name": "cloudflare-docker-mirrors",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --env development",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.0.7",
		"wrangler": "^4.40.3"
	},
	"dependencies": {
		"lodash": "^4.17.21"
	}
}
